# Disabled — Aniware now provisions iframe sources on-demand from free
# public CDNs (vidsrc, 2embed, miruro). No torrent/transcode/upload pipeline
# is needed. Kept as a no-op so the file stays in repo history.
name: Aniware Ingest (disabled)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ingest pipeline disabled. Sources are auto-provisioned in-app."
